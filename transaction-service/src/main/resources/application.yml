server:
  port: 8082

spring:
  application:
    name: transaction-service

banking:
  account-service:
    base-url: http://localhost:8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
