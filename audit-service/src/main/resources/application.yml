server:
  port: 8084

spring:
  application:
    name: audit-service

banking:
  account-service:
    base-url: http://localhost:8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
